name: chunked_link_framed_chunker

root:
  name: top
  deps:
    - nsl_bnoc.framed
    - nsl_bnoc.chunked_link
    - nsl_bnoc.pipe
    - nsl_bnoc.testing
    - nsl_data.text
    - nsl_simulation.driver
  sources:
    - file_type: vhdl
      files:
        - src/tb.vhd

output:
  - name: simulation
    topcell: tb
    filter_vars:
      hwdep: simulation
    backend_config:
      gbs.builtin.ghdl:
        success_regex: "Terminating with error level: 0"
    outputs:
      - type: simulation-log
        path: simulation.log
