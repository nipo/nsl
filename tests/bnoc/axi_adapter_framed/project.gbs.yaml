name: bnoc_axi_adapter_framed

root:
  name: top
  deps:
    - nsl_bnoc.framed
    - nsl_bnoc.testing
    - nsl_bnoc.axi_adapter
    - nsl_amba.axi4_stream
    - nsl_data.bytestream
    - nsl_data.prbs
    - nsl_simulation.logging
    - nsl_simulation.driver
  sources:
    - file_type: vhdl
      files:
        - src/tb.vhd

output:
  - name: simulation
    topcell: tb
    backend_config:
      gbs.builtin.ghdl:
        success_regex: "Terminating with error level: 0"
    outputs:
      - type: simulation-log
        path: simulation.log
