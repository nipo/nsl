name: bnoc_framed_router

root:
  name: top
  deps:
    - nsl_bnoc.framed
    - nsl_bnoc.testing
    - nsl_data.text
    - nsl_simulation.driver
    - nsl_bnoc.testing
  sources:
    - file_type: vhdl
      files:
        - src/tb.vhd

output:
  - name: simulation
    topcell: tb
    filter_vars:
      hwdep: simulation
    backend_config:
      gbs.builtin.ghdl:
        success_regex: "Terminating with error level: 0"
    outputs:
      - type: simulation-log
        path: simulation.log
