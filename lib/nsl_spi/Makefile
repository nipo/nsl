packages += spi
packages += shift_register
packages += slave
packages += transactor
packages += router
packages += fifo_transport
packages += io_extender
packages += flash
packages += testing
packages += source
