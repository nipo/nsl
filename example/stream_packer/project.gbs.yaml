name: stream_packer

root:
  name: top
  deps:
    - nsl_amba.axi4_stream
    - nsl_amba.stream_processing
    - nsl_data.bytestream
    - nsl_data.prbs
    - nsl_hwdep.clock
    - nsl_hwdep.reset
    - nsl_logic.logic
  sources:
    - file_type: vhdl
      files:
        - src/boundary.vhd
    - file_type: gowin-cst
      files:
        - ../sipeed_tang_25k/pinout/done_led.cst

output:
  - name: synthesis
    topcell: boundary
    target:
      part: GW5A-LV25MG121NC1/I0#A
      use_as_gpio:
        - sspi
        - done
        - ready
        - cpu
    outputs:
      - type: gowin-fs
        path: stream_packer.fs
